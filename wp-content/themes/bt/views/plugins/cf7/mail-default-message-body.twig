<table border="1px" cellspacing="0" cellpadding="3">
  <tr>
    <th>{{ esc_html__('שם מלא:', 'bt') }}</th>
    <td>{{ '[full_name]' }}</td>
  </tr>
  <tr>
    <th>{{ esc_html__('טלפון:', 'bt') }}</th>
    <td>{{ '[phone]' }}</td>
  </tr>
  <tr>
    <th>{{ esc_html__('אימייל:', 'bt') }}</th>
    <td>{{ '[email]' }}</td>
  </tr>
  <tr>
    <th>{{ esc_html__('הודעה:', 'bt') }}</th>
    <td>{{ '[message]' }}</td>
  </tr>
</table>

-----

{{ esc_html__('אימייל זה נשלח מטופס יצירת קשר ב', 'bt') ~ '[_site_title] ([_site_url])' }}